// Generated by CoffeeScript 1.7.1
module.exports = function(grunt) {
  var angularInjector;
  angularInjector = require('angular-injector');
  return grunt.registerMultiTask('angular-injector', 'Annotates angular.js files with dependency injection arrays', function() {
    return this.files.forEach((function(_this) {
      return function(files) {
        return files.src.forEach(function(filepath) {
          if (!grunt.file.exists(filepath)) {
            return grunt.log.warn("Source file \"" + filepath + "\" not found.");
          }
          return grunt.file.write(files.dest, angularInjector.annotate(grunt.file.read(filepath), _this.options()));
        });
      };
    })(this));
  });
};
